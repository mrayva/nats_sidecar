# nats_sidecar configuration example

nats_address: "127.0.0.1"
nats_port: 4222

# Input: core NATS subjects carrying binary-encoded messages, all sharing
# this file's one attribute schema below. Every message from every listed
# subject is matched against the same subscription tree. Add more entries
# to subscribe to multiple subjects at once:
#   input_subjects: ["sensor.data", "sensor.data.backup"]
# output_prefix (below) must be set explicitly whenever more than one
# input subject is listed - with exactly one, it defaults to that subject.
input_subjects: ["sensor.data"]
# format's own built-in default is `arrow` (columnar batches straight from pg_arrow's
# rows_to_arrow()) - it REQUIRES every connection to set columnar: true (see "Arrow columnar
# input" in README.md). This example overrides it to msgpack, since row mode needs no columnar
# setup and is the simplest way to get a first instance running.
format: msgpack          # msgpack | cbor | flexbuffers | zera | ion | bson | beve | arrow (default)
# input_queue_group: "sidecar-group"  # uncomment to load-balance across instances

# Matching engine - atree/betree accept the same expression syntax, except "is not empty" which
# only atree supports (see README's Expression Syntax section); pstree additionally supports a
# native `decimal` attribute type the other two reject. Switching this after subscriptions exist
# won't crash startup, but any persisted subscription that used "is not empty" will fail to
# restore under engine: betree (logged as a warning and skipped, not lost - the lease record
# itself is untouched). pstree is the fastest of the three at essentially every tested workload
# shape (see README.md's pstree performance investigation) and the default here.
engine: pstree             # atree | betree | pstree (default)

# Output: matched messages published to <output_prefix>.<BE-ID>
output_prefix: "sensor.filtered"
# output_format: msgpack  # optional; must differ from `format` when format: arrow (defaults to
                           # msgpack in that case, which is why this example - format: msgpack
                           # above - leaves it unset: "same as format" already resolves to msgpack)

# Durable JetStream output (opt-in, off by default - zero behavior change and zero new NATS/
# JetStream traffic unless enabled). Real use case: one deployment runs BOTH a `mode: core`
# input connection (periodic full-table "flush" republishes, already tolerant of at-most-once
# since the whole table resends soon) and a `mode: js` input connection (genuinely incremental
# updates that must not be lost) - see "Multiple input connections" below. With this enabled, a
# matched row sourced from the js-mode connection routes to a SEPARATE, durable subject instead
# of the always-on core/flush subject above, so clients can create their own durable JetStream
# consumer to catch up on missed updates, while flush-sourced matches keep going out exactly as
# before regardless. A deployment using `mode: js` input purely for ingestion durability -
# unrelated to this feature - is unaffected unless it also opts in here.
# output_stream_enabled: true
# output_updates_prefix: "sensor.filtered.updates"  # default: "<output_prefix>.updates" if unset
# output_stream_name: "sidecar-output"
# output_stream_storage: "file"          # "file" (real durability) | "memory" (isolation testing only)
# output_stream_max_age_seconds: 3600    # bounded catch-up window
# output_stream_max_bytes: 1073741824    # hard cap alongside the age limit (1GiB)
#
# Deliberately "limits" retention (age/byte-bound, ack-independent), not "workqueue" like the
# input stream: an unbounded number of independent per-client durable consumers each replay the
# same messages on their own schedule, and workqueue retention deletes a message once ANY one of
# them acks it - which would silently break catch-up for every other client still behind. See
# README.md's "Durable JetStream output" section for the full design and a real bug this exact
# design decision was checked against empirically (a naive durable-consumer-name choice that
# looked fine on paper but wasn't a valid JetStream name at all).

# Subscription management endpoints
subscribe_subject: "sidecar.subscribe"
unsubscribe_subject: "sidecar.unsubscribe"

# Soft-state lease settings (NATS KV). A client's only renewal signal is
# calling sidecar.subscribe again for the same expression/client_id (cheap -
# just a lease-holder-set insert, no tree mutation - see subscription_manager
# ::subscribe()'s "reused expression" branch); there is no separate heartbeat
# endpoint. If a client stops renewing (crash, forgot to unsubscribe, etc.),
# its subscription is torn down somewhere between lease_ttl_seconds and
# lease_ttl_seconds + lease_check_interval_seconds later - that window is
# this sidecar's "grace period" for a gone client. The two knobs trade off
# directly: shorter values mean faster cleanup of dead clients but require
# clients to re-subscribe that often just to stay alive; the defaults below
# favor rarely-needed renewal over fast cleanup (a dead client's resources
# can linger up to an hour). See README.md's "Grace period / dead-client
# cleanup" section for the full reasoning, including why a short
# lease_check_interval_seconds is now cheap even under high subscription
# churn (see the "High-churn-under-load" section's remove() fix).
lease_bucket: "sidecar-leases"
lease_ttl_seconds: 3600           # bucket is created/validated with this TTL
lease_check_interval_seconds: 60  # reconcile expired/deleted lease keys

# Example "responsive grace period" profile instead of the defaults above -
# a dead client's subscription is cleaned up within ~15-20s instead of up to
# ~1 hour, at the cost of clients needing to re-subscribe every ~10-15s to
# stay alive. Uncomment and tune for your own deployment's actual renewal
# cadence - these are illustrative starting points, not a recommended default.
# lease_ttl_seconds: 15
# lease_check_interval_seconds: 5

# A-Tree attribute schema - defines the fields available for boolean expressions
# Clients write expressions like: temperature > 30.0 AND location = "warehouse-A"
attributes:
  - name: temperature
    type: float
  - name: humidity
    type: float
  - name: location
    type: string
  - name: severity
    type: integer
  - name: active
    type: boolean
  - name: tags
    type: string_list

# Operational
stats_interval_seconds: 10
log_level: info

# Worker threads for parallel message processing
# 0 = use std::thread::hardware_concurrency()
# worker_threads: 4

# Bounded input queue. Newest messages are dropped when either limit is hit.
input_queue_max_messages: 10000
input_queue_max_bytes: 67108864   # 64 MiB

# Bounded output work. Each task may publish to multiple matching subjects.
publish_max_inflight: 1024
publish_backpressure_timeout_ms: 5000
